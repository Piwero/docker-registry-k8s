---
replicaCount: 1
persistence:
  enabled: true
  size: 60Gi
  deleteEnabled: true
  storageClass: csi-cinder-classic
  existingClaim: docker-registry-pv-claim
secrets:
  htpasswd: < password from the content of the file which was generated by the gen-pass.sh script (./registry-cred/htpasswd) >